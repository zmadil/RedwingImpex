import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact & Request a Quote — Redwing Impex",
  description:
    "Get in touch with Redwing Impex for wholesale Himalayan salt pricing, custom orders, and private labeling. Based in Istanbul, shipping globally.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
